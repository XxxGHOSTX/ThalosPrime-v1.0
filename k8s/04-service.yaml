apiVersion: v1
kind: Service
metadata:
  name: thalos-prime
  namespace: thalos-prime
  labels:
    app: thalos-prime
spec:
  type: ClusterIP
  ports:
  - port: 80
    targetPort: 8080
    protocol: TCP
    name: http
  selector:
    app: thalos-prime
    component: session-manager
